# pitch-plus-app

Pitch+ analytics dashboard — React + TypeScript + Vite.

## Setup

```bash
git clone https://github.com/fluxodyssey/pitch-plus-app.git
cd pitch-plus-app
npm install
npm run dev
```

App runs at **http://localhost:5173**
